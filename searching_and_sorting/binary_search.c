#include <stdio.h>

int	main(void)
{
	int	arr[] ={1,12,21,23,43,45,54,56,65,67};
	int	len;
	int	find;
	int	nbr;
	int mid;

	mid = (0 + (len - 1)) / 2;
	find = 0;
	len = sizeof(arr) / sizeof(arr[0]);

	scanf("%d",&nbr);

	if (nbr == mid)
		return (1);
	if (nbr < arr[len - 1])
		






}
